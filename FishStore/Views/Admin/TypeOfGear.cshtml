@model IEnumerable<FishStore.Entities.Products.ProductDicts.TypeOfGear>
@{
    Layout = "_AdminLayout";
}

<h2>Типы снастей</h2>
<a class="text-primary" asp-controller="Admin" asp-action="AddTypeOfGear">Добавить тип Снасти</a>
<form method="get" asp-action="TypeOfGear" asp-controller="Admin">
    <input placeholder="Снасть" type="text" name="searchText"/>
    <button type="submit" class="btn btn-primary">Поиск</button>
</form>
<table class="table">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Наименование</th>
        </tr>
    </thead>
    <tbody>
        @foreach(FishStore.Entities.Products.ProductDicts.TypeOfGear type in Model){
            <tr>
                <th scope="row">@type.ID</th>
                <td>@type.Name</td>
                <td>
                    @Html.ActionLink("Изменить", "EditTypeOfGear", new {id = type.ID }) |
                    @Html.ActionLink("Удалить", "DeleteTypeOfGear", new {id = type.ID })
                </td>
            </tr>
        }
    </tbody>
</table>