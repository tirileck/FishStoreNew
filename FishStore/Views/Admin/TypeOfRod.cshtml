@model IEnumerable<FishStore.Entities.Products.ProductDicts.TypeOfRod>
@{
    Layout = "_AdminLayout";
}

<h2>Типы удочек</h2>
<a class="text-primary" asp-controller="Admin" asp-action="AddTypeOfRod">Добавить тип удочек</a>
<form method="get" asp-action="TypeOfRod" asp-controller="Admin">
    <input placeholder="Удочка" type="text" name="searchText"/>
    <button type="submit" class="btn btn-primary">Поиск</button>
</form>
<table class="table">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Наименование</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var type in Model){
            <tr>
                <th scope="row">@type.ID</th>
                <td>@type.Name</td>
                <td>
                    @Html.ActionLink("Изменить", "EditTypeOfRod", new {id = type.ID }) |
                    @Html.ActionLink("Удалить", "DeleteTypeOfRod", new {id = type.ID })
                </td>
            </tr>
        }
    </tbody>
</table>