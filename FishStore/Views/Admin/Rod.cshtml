@model IEnumerable<FishStore.Entities.Products.Rod>
@{
    Layout = "_AdminLayout";
}

<h2>Удочки</h2>
<a class="text-primary" asp-controller="Admin" asp-action="AddRod">Добавить удочку</a>
<form method="get" asp-action="Rod" asp-controller="Admin">
    <input placeholder="Удочка" type="text" name="searchText"/>
    <button type="submit" class="btn btn-primary">Поиск</button>
</form>
<table class="table">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Наименование</th>
            <th scope="col">Тип удочки</th>
            <th scope="col">Размер</th>
            <th scope="col">Вес</th>
            <th scope="col">Цена</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var type in Model){
            <tr>
                <th scope="row">@type.ID</th>
                <td>@type.Name</td>
                <td>@type.TypeOfRod.Name</td>
                <td>@type.Size</td>
                <td>@type.Weight</td>
                <td>@type.Cost р.</td>
                <td>
                    @Html.ActionLink("Изменить", "EditRod", new {id = type.ID }) |
                    @Html.ActionLink("Удалить", "DeleteRod", new {id = type.ID })
                </td>
            </tr>
        }
    </tbody>
</table>